# 2025-02-08

## ClearBoard Development

### UI Changes
- Redesigned toolbar with **3 colors only** (Red, Black, White)
- Colors now **vertical** with 12px spacing
- Selected color shows **light blue ring** (like macOS WiFi icon)
- Toolbar height increased to 700px
- Removed text from color buttons
- Clear Drawing button (trash icon) added

### Bug Fixes
- Fixed layout recursion error: "It's not legal to call -layoutSubtreeIfNeeded on a view which is already being laid out"
- Solution: Wrapped layer updates in `DispatchQueue.main.async` + `CATransaction.setDisableActions(true)`

### Build Status
- macOS build succeeds
- Resolved "task name port" error (sandbox/debugging issue)

### iPad Support Decision
- Chose **Option 2: Universal project** (macOS + iPadOS) over separate targets
- Rationale: Shared drawing logic, existing iPadOS views, easier maintenance
- TODO: Update project.yml to include iPadOS sources

### Files Modified
- `/Sources/Overlay/OverlayEngine.swift` - toolbar, colors, layout fixes
- `/Sources/Views/macOS/ToolbarView.swift`
